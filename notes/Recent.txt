# Recent Changes

Expanded and removed Kate Test.zip
It became TEST folder. 
Renamed TEST to app.
Everything still works with python3 run.py

All images are now in app/static/Images, so deleted the old /Images

Created new folder: models.
Moved AccessRequest.py and all the other py files into it.
Created models/__init__.py and imported the classes into it, and re-exported them.
This replaces the former models.py.